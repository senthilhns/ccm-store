# AWS Configuration
aws_region = "ap-south-1"

# DynamoDB Table Configuration
table_name = "example-dynamodb-table"
environment = "test"

# Encryption (uncomment and set to use a specific KMS key)
# kms_key_arn = "arn:aws:kms:ap-south-1:123456789012:key/abcd1234-..."

# Enable point-in-time recovery (increases cost)
# enable_point_in_time_recovery = true

# Additional Tags
tags = {
  Name        = "sample-dynamodb-table"
  Environment = "test"
  Purpose    = "policy-test"
  ManagedBy  = "terraform"
}
